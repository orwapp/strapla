.page-header
  = link_to jobs_path, :class => 'btn' do
    %i.icon-list-alt
    Back
  = link_to edit_job_path(@job), :class => 'btn btn-primary' do
    %i.icon-pencil.icon-white
    Edit
  %h1 Listing jobs

%dl
