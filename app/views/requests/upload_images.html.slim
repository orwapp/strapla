- if @request.images.present?
  h2 Images
  .container.border
    - @request.images.each do |image|
      = link_to image_tag( image.image_url, size: '500x500', 
        class: 'thumbnail pull-left span4'), image.image_url, target: :blank



h3 Upload images 

= simple_form_for([@request, @request.images.new]) do |f|
  = f.hidden_field :return_to_page, 
    value: "/requests/#{@request.id}/upload_images"
  = f.input :title
  = f.input :description

  = f.file_field :image, input_html: { class: 'span8 help_text'}
  br
  br
  br
  br
  br
  = f.submit 'Upload image', class: 'btn btn-info', 
    data: { disable_with: 'Uploading...' }

= link_to '  Back to the request', @request, class: 'pull_right'


